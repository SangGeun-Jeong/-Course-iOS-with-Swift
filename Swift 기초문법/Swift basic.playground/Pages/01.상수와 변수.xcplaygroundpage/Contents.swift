import Foundation

// 상수 : 변하지 않는 값.
// let 상수명 : 데이터 타입 = 값
let a: Int = 100

// 변수 : 변하는 값.
// var 변수명 : 데이터 타입 = 값
var b: Int = 200


/*
 상수는 언제 사용할까?
 
 프로그램에서 바뀌지 않아야 하는 값을 설정해 줄 때 사용.
 실수로 값이 변경되면 프로그램에 큰 영향을 미치는 값에 사용.(ex.핸드폰 앱의 전체용량)
 */
